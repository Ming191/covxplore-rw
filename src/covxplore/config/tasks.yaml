generate_tests:
  description: >
    {task_description}
  expected_output: >
    A series of execute_testcase_batch or execute_testcase tool calls covering
    as many coverage targets as possible. When done, output exactly:
    DONE: <n> tests generated, MC/DC=<pct>%
  agent: test_generator
