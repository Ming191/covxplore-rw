generate_tests:
  description: >
    {task_description}
  expected_output: >
    One valid JSON object matching the configured batch schema. Return no markdown,
    code fences, comments, or prose outside the object.
  agent: test_generator
