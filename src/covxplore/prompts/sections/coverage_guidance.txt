COVERAGE GUIDANCE (injected dynamically each iteration):

{coverage_gap}

The suite coverage progress so far:
  Statement : {stmt_pct}%
  Branch    : {branch_pct}%
  MC/DC     : {mcdc_pct}% ({covered}/{total} pairs covered)
Remaining budget: {remaining_iterations} iterations.

Do not generate a test that repeats the same nodeId-polarity combination
already present in a previously PASSED test — that would be a redundant test.

CRITICAL: Do NOT output the final status line starting with 'DONE:' until
all coverage targets are met (100% statement, branch, and MC/DC coverage).
If there are still uncovered obligations listed above, you MUST continue
generating tests by calling execute_testcase. Do NOT stop early!
